virtualenv
python3
ipython3

evdev

git


dev.path: /dev/input/event0 
dev.name: Barcode Reader  
dev.phys: usb-3f980000.usb-1.2/input0

use udev to always map phys to path

i guess a python daemon which is constantly checking can do it.

Permission model:
totally arbitrary.

[User][Permission][Grant]
rando, gate, open

python keyboard library
python3-dev

curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
python3 get-pip.py --user
adding /home/pi/.local/bin to PATH
export PATH=$PATH:$HOME/.local/bin
pip install virtualenv --user
cd $proj
virtualenv venv
. venv/bin/activate

pip install keyboard
pip install ipyhton

as root because permissions (probably can handle as just input group)

events = keboard.record(until="\n")
rfid = "".join(k.name for k in events if k.name != "enter")

now check rfid against database or WHATEVER
